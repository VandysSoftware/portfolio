# OS cruft
.DS_Store
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp

# Nothing is built here, but keep these out if tooling ever shows up
node_modules/
dist/
.env
.env.*

# Design-session mockups. Currently also holds the personal-use-only build of
# Divebar Vintage — that file must not be committed or deployed.
.superpowers/
